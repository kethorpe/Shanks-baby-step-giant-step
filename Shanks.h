#include <iostream>
#include <cstdlib>
#include <stdio.h>
#include <string.h>

using namespace std;

class Shanks
{
  public:
    Shanks(int n, int p, int l);
    int power(int x, int y, int p);
    int getInverse(int n, int p);
};
